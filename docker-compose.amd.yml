# AMD GPU Support (ROCm)
services:
  translator:
    build:
      context: .
      dockerfile: Dockerfile.amd
    devices:
      - /dev/kfd
      - /dev/dri
    group_add:
      - video
    environment:
      - HSA_OVERRIDE_GFX_VERSION=11.0.0
